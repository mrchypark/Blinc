//! Desktop window management

/// A desktop window
pub struct DesktopWindow {
    // TODO: winit window
}

impl DesktopWindow {
    pub fn new(_title: &str, _width: u32, _height: u32) -> Self {
        Self {}
    }
}
